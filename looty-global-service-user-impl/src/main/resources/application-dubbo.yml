dubbo:
  application:
    name: dubbo-looty-user-provider
    enable-file-cache: false
    qos-enable: false
    check-serializable: true
  protocol:
    name: dubbo
    port: -1
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
  provider:
    filter: dubboExceptionFilter
  config-center:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
  metadata-report:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
